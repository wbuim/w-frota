<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Detalhes do Veículo - W-Frota</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f4f6f9; margin: 0; padding: 20px; color: #333; }
        .container { max-width: 1000px; margin: 0 auto; background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        
        /* CABEÇALHO */
        .header { display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #eee; padding-bottom: 15px; margin-bottom: 20px; }
        h1 { margin: 0; color: #2c3e50; font-size: 24px; }
        .btn-voltar { text-decoration: none; color: #666; font-weight: 600; display: flex; align-items: center; gap: 5px; }
        .btn-voltar:hover { color: #007bff; }

        /* CARDS RESUMO */
        .resumo-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; margin-bottom: 30px; }
        .card-resumo { background: #f8f9fa; padding: 15px; border-radius: 8px; text-align: center; border: 1px solid #e9ecef; }
        .card-resumo h3 { margin: 0; font-size: 12px; color: #666; text-transform: uppercase; }
        .card-resumo p { margin: 5px 0 0; font-size: 20px; font-weight: 700; color: #333; }

        /* TABELAS */
        h2 { margin-top: 40px; color: #444; font-size: 18px; border-left: 5px solid #007bff; padding-left: 10px; margin-bottom: 15px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background: #007bff; color: white; padding: 10px; text-align: left; font-size: 13px; }
        td { padding: 10px; border-bottom: 1px solid #eee; font-size: 14px; color: #333; vertical-align: middle; }
        tr:hover { background-color: #f1f1f1; }

        /* BOTÃO FOTO */
        .btn-foto {
            display: inline-flex; align-items: center; gap: 5px;
            background: #e2e6ea; color: #333; text-decoration: none;
            padding: 4px 10px; border-radius: 20px; font-size: 12px; font-weight: bold;
            transition: 0.2s; border: 1px solid #ced4da;
        }
        .btn-foto:hover { background: #007bff; color: white; border-color: #007bff; }

        /* FORMULÁRIOS ADMIN (Escondidos por padrão) */
        details { background: #fff3cd; padding: 10px; border-radius: 5px; margin-bottom: 10px; }
        summary { cursor: pointer; font-weight: bold; color: #856404; }
        .form-admin { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 10px; }
        input, select { padding: 8px; border: 1px solid #ccc; border-radius: 4px; width: 100%; box-sizing: border-box; }
        button { background: #28a745; color: white; border: none; padding: 10px; border-radius: 4px; cursor: pointer; font-weight: bold; }
        button:hover { opacity: 0.9; }

    </style>
</head>
<body>

<div class="container">
    <div class="header">
        <h1>🚙 <%= veiculo.modelo %> <span style="font-weight:normal; color:#777;">(<%= veiculo.placa %>)</span></h1>
        <a href="/" class="btn-voltar">← Voltar</a>
    </div>

    <div class="resumo-grid">
        <div class="card-resumo">
            <h3>KM Atual</h3>
            <p><%= veiculo.km_atual %> km</p>
        </div>
        <div class="card-resumo">
            <h3>Status</h3>
            <p><%= veiculo.status %></p>
        </div>
        <div class="card-resumo" style="border-bottom: 3px solid #ffc107;">
            <h3>Gasto Combustível</h3>
            <p>R$ <%= totalCombustivel.toFixed(2) %></p>
        </div>
        <div class="card-resumo" style="border-bottom: 3px solid #dc3545;">
            <h3>Gasto Oficina</h3>
            <p>R$ <%= totalManutencao.toFixed(2) %></p>
        </div>
    </div>

    <details>
        <summary>🔧 Ferramentas Administrativas (Correção de KM e Troca de Óleo)</summary>
        
        <div style="margin-top: 15px; border-top: 1px solid #e0e0e0; padding-top: 10px;">
            <strong>Ajustar Hodômetro (Correção)</strong>
            <form action="/admin/ajustar_km" method="POST" class="form-admin" style="grid-template-columns: 1fr 2fr 1fr;">
                <input type="hidden" name="veiculo_id" value="<%= veiculo.id %>">
                <input type="number" name="novo_km" placeholder="Novo KM" required>
                <input type="text" name="justificativa" placeholder="Motivo (ex: Painel quebrou)" required>
                <button type="submit" style="background: #6c757d;">Corrigir</button>
            </form>
        </div>

        <div style="margin-top: 15px; border-top: 1px solid #e0e0e0; padding-top: 10px;">
            <strong>Registrar Troca de Óleo</strong>
            <form action="/admin/registrar_troca_oleo" method="POST" class="form-admin">
                <input type="hidden" name="veiculo_id" value="<%= veiculo.id %>">
                <input type="number" name="km_troca" placeholder="KM da Troca" value="<%= veiculo.km_atual %>" required>
                <input type="text" name="tipo_oleo" placeholder="Tipo do Óleo (ex: 5W30)" required>
                <input type="number" step="0.1" name="litros" placeholder="Litros">
                <input type="number" step="0.01" name="valor_total" placeholder="Valor Total (R$)">
                <input type="text" name="posto" placeholder="Local da Troca">
                <input type="text" name="observacao" placeholder="Obs (Filtros, etc)">
                <button type="submit" style="grid-column: span 2;">Registrar Troca</button>
            </form>
        </div>
    </details>

    <h2>🗺️ Histórico de Viagens (Últimas 5)</h2>
    <% if(viagens.length === 0) { %> <p>Nenhuma viagem registrada.</p> <% } else { %>
    <table>
        <thead><tr><th>Data</th><th>Motorista</th><th>Destino</th><th>KM Rodados</th></tr></thead>
        <tbody>
            <% viagens.slice(0, 5).forEach(v => { %>
            <tr>
                <td><%= new Date(v.data_saida).toLocaleDateString('pt-BR') %></td>
                <td><%= v.motorista %></td>
                <td><%= v.destino %></td>
                <td><strong><%= v.km_rodados %> km</strong></td>
            </tr>
            <% }) %>
        </tbody>
    </table>
    <% } %>

    <h2>⛽ Histórico de Abastecimentos</h2>
    <% if(abastecimentos.length === 0) { %> <p>Nenhum abastecimento.</p> <% } else { %>
    <table>
        <thead><tr><th>Data</th><th>Motorista</th><th>Detalhes</th><th>Valor</th><th>Comprovante</th></tr></thead>
        <tbody>
            <% abastecimentos.forEach(a => { %>
            <tr>
                <td><%= new Date(a.data).toLocaleDateString('pt-BR') %></td>
                <td><%= a.motorista %></td>
                <td><%= a.litros %>L (<%= a.tipo_combustivel %>) <br> <small><%= a.posto %></small></td>
                <td>R$ <%= a.valor_total.toFixed(2) %></td>
                <td>
                    <% if (a.foto) { %>
                        <a href="/uploads/<%= a.foto %>" target="_blank" class="btn-foto">
                            📎 Ver Foto
                        </a>
                    <% } else { %>
                        <span style="color:#ccc; font-size:12px;">-</span>
                    <% } %>
                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>
    <% } %>

    <h2>🛠️ Histórico de Manutenção</h2>
    <% if(manutencoes.length === 0) { %> <p>Nenhuma manutenção.</p> <% } else { %>
    <table>
        <thead><tr><th>Data</th><th>Oficina</th><th>Serviço</th><th>Valor</th><th>Comprovante</th></tr></thead>
        <tbody>
            <% manutencoes.forEach(m => { %>
            <tr>
                <td><%= new Date(m.data).toLocaleDateString('pt-BR') %></td>
                <td><%= m.oficina %></td>
                <td><%= m.descricao %></td>
                <td>R$ <%= m.valor.toFixed(2) %></td>
                <td>
                    <% if (m.foto) { %>
                        <a href="/uploads/<%= m.foto %>" target="_blank" class="btn-foto">
                            📎 Ver Nota
                        </a>
                    <% } else { %>
                        <span style="color:#ccc; font-size:12px;">-</span>
                    <% } %>
                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>
    <% } %>

</div>

</body>
</html>